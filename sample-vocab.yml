# This configuration file can be used as an input by [@inrupt/artifact-generator]
# to generate code artifacts from RDF vocabularies.
# 
# Generated by the artifact generator [@inrupt/artifact-generator], version [1.0.4]
# on 'Monday, December 13, 2021 4:01 PM'.

# The artifact name is shared among all the artifacts.
artifactName: Groceries
artifactGeneratorVersion: 1.0.4
versioning:
  type: git
  url: https://github.com/jbrocher/groceries-ontology
  versioningTemplates:
    - template: "templates/.gitignore.hbs"
      fileName: ".gitignore"
  

artifactToGenerate:
  - programmingLanguage: "JavaScript"
    artifactVersion: "0.1.0"
    artifactNamePrefix: "gro"
    artifactDirectoryName: "JavaScript"
    templateInternal: "solidCommonVocabDependent/javascript/vocab.hbs"
    sourceFileExtension: "js"
    # Currently we're just adding terms as they occur in vocabs, and not all possible keywords.
    languageKeywordsToUnderscore:
      - "class"
      - "abstract"
      - "default"
    packaging:
      - packagingTool: "NPM"
        npmModuleScope: "jbrocher"
        publish:
        - key: "local"
          command: "npm publish --registry http://localhost:4873"
        - key: "remote"
          command: "npm publish"
        packagingTemplates: 
          - template: solidCommonVocabDependent/javascript/index.hbs
            fileName: index.js
          - template: solidCommonVocabDependent/javascript/package.hbs
            fileName: package.json

vocabList:
  # The following is the list of IRI and/or paths to read when building the artifact.
  - inputResources:
      - "./groceries.ttl"
    # This option is used as a name for the vocabulary, e.g. EXAMPLE.java or EXAMPLE.js
    # If not provided, the generator will look for the vann:preferredNamespacePrefix property,
    # and otherwise will propose a default based on the domain name.
